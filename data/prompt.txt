作为一位专业的智慧农业助手，你的任务是从给定的上下文中提出多条多样化的任务指令。这些任务指令将被给予一个GPT模型，我们将评估GPT模型完成这些指令的能力。
以下是要求：
1、尝试不要重复每条指令的动词，以最大化多样性。
2、指令使用的语言也应该具有多样性。例如，你应该结合问题和命令式指令。
3、指令的类型应该多样化。列表应该包括开放式生成、分类、编辑等多种类型的任务。
4、GPT语言模型应该能够完成指令。例如，不要要求助手创建任何视觉或音频输出。另一个例子，不要要求助手在下午5点叫醒你或设置提醒，因为它不能执行任何动作。
5、指令应该用保证用中文。
6、指令应该是1到2句话长。可以是命令句或者是问题句。
7、你应该为指令生成一个适当的输入。输入字段应该包含为指令提供的具体示例。它应该涉及现实数据，不应该包含简单的占位符。输入应该提供足够的内容使指令具有挑战性，但理想情况下不应超过100个单词。
8、并非所有指令都需要输入。例如，当指令询问一些一般信息时，如“世界上最高的山峰是什么”，就可以直接回答输出而无需将上下文信息放入输入中。在这种情况下，我们将输入字段设置为空。
9、输出应该是对指令及其输入的适当回应。
10、请在任务指令中适当位置加入‘苹果’字样，以明确指出上下文内容与苹果相关。例如，将‘腐烂病’修改为‘苹果腐烂病’。
11、请尽可能覆盖上下文资料中的所有具体信息。
12、请按照以下标准的JSON格式来回答,不要包含任何其他非JSON内容：
{
      "instruction": "",
      "input": "",     
      "output":"" 
}

给定的上下文：